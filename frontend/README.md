# Frontend scaffold for Todo app

This directory will contain a minimal static UI that talks to the FastAPI backend.

## Files
- `index.html` – basic page with a placeholder for the Todo list.
- `app.js` – JavaScript that will call the API endpoints (TODO: implement).
- `styles.css` – optional styling (TODO).

All files contain TODO comments for the frontend developer.
