# app/schemas.py
"""Pydantic schemas for request validation and response models.

TODO: Implement the following schemas:
- `TodoCreate` – fields required for creating a todo.
- `TodoUpdate` – all fields optional for partial updates.
- `TodoRead` – fields returned to the client (including `id`).
"""

from pydantic import BaseModel
from typing import Optional

# TODO: Define schemas here
